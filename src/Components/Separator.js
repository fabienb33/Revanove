const Separator = (props) => {
    return (
        <div className="boxSeparator">
            <h3 className="textSeparator">{props.title}</h3>
            <p className="separator"></p>
        </div>
    )}
export default Separator